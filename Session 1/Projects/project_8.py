"""
Project 8: Multi-Input Calculator with Data Processing
Description: Create an advanced calculator that processes multiple inputs using:
-> The `get_multiple_numbers()` function to collect several numbers at once
-> Variable swapping techniques to sort the numbers
-> Type conversion between lists, tuples, and sets for different operations
-> Use of `zip()` and dictionary conversion for mapping operations
-> Operations that demonstrate implicit type conversion (mixing ints and floats)
-> Display results with different formatting styles (percentage, currency, etc.)
-> Save calculation history using the `print()` `file=` parameter

"""